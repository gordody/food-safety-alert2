# TODO

* Format alerts so the product title is more prominent than the rest
* Move utility functions like `formatDate` to their own folder / file
* Wire each alert item to a dedicated details view route.
* Add location-aware filtering (state/distribution pattern) before rendering.
* Connect the Customize Alerts button to a real filter/preferences screen.
