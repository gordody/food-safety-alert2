{
  "name": "food-safety-recall-alert-app",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "android:dev": "TAURI_DEV_HOST=${TAURI_DEV_HOST:-$(ipconfig getifaddr en0 || ipconfig getifaddr en1)} && [ -n \"$TAURI_DEV_HOST\" ] || (echo 'Could not detect local IP. Set TAURI_DEV_HOST manually.' && exit 1) && echo \"Using TAURI_DEV_HOST=$TAURI_DEV_HOST\" && tauri android dev",
    "ios:dev": "TAURI_DEV_HOST=${TAURI_DEV_HOST:-$(ipconfig getifaddr en0 || ipconfig getifaddr en1)} && [ -n \"$TAURI_DEV_HOST\" ] || (echo 'Could not detect local IP. Set TAURI_DEV_HOST manually.' && exit 1) && echo \"Using TAURI_DEV_HOST=$TAURI_DEV_HOST\" && tauri ios dev",
    "ios:dev:simulator": "tauri ios dev \"iPhone 17\"",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "coverage": "vitest run --coverage",
    "tauri": "tauri"
  },
  "license": "MIT",
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-geolocation": "~2.3.2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-store": "~2.4.2"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.9.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2",
    "@vitest/coverage-v8": "^4.1.0",
    "jsdom": "^29.0.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.0"
  }
}
